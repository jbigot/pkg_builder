* config & CLI parameters
* scheduling by generating github tasks
* provide github actions
* split into modules
* support other archs
* use python for ./build
* support disabling on specific distro

deb:
* provide key in repo
* provide key package
* provide repo installer script
* handle dev. key in addition to repo key
* handle arch & profile for deps
* use package from upper distro if possible
* provide all info when publishing, including suite
* support http/local URLs for debian & orig

rpm:
* generate final repo
* generate .repo file
* sign packages
* distro DB from reliable source
